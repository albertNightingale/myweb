version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm i
    build:
      commands:
        - npm run prod
  artifacts:
    baseDirectory: build/static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
